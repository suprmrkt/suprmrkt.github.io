var horizontalCount; var verticalCount; var speed =0.5; var initialSpacing = 5; var waveSize = 300; var lineLength = 20; var spacing = 15; function setup() {   createCanvas(windowWidth, windowHeight);   horizontalCount = round(width / spacing);   verticalCount = round(width / spacing);    waveSize=random(300,700); } function draw() {   background("#000000");   strokeWeight(0.5);   strokeCap(PROJECT);   for (var i = 0; i <= horizontalCount; i++) {     for (var j = 0; j <= verticalCount; j++) {       var inter = map(i, 0, width, 0, spacing);       var c = lerpColor(color("rgba(50, 150, 250, 0.2)"), color("rgba(50, 239, 213, 0.8)"), inter);       stroke(c);             var lineX = i*spacing + initialSpacing;       var lineY = j*spacing + initialSpacing;       var dx = mouseX - i*spacing;       var dy = mouseY - j*spacing;       var r = atan2(dy, dx);       push();        strokeWeight(0.5/r);       translate(i*spacing + initialSpacing, j*spacing + initialSpacing);       rotate(radians(frameCount * speed + i * 10 + j * 10) + r);       translate(-i*spacing - initialSpacing, -j*spacing - initialSpacing);       line(lineX - lineLength*r + waveSize, lineY, lineX + lineLength + waveSize, lineY);       pop();     }   } } 
